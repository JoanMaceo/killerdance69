#include <math.h>
#include <stdlib.h>
#include <stdio.h>
#include 'Funcions.h' 

int newton( double *x, double tol, int iter){
    for (int i = 0; i < iter; i ++){
        if (x)
        x = x - fun(x)/der(k);
    }
}